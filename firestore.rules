rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All writes go through Firebase Admin SDK on the server.
    // Client reads are blocked — dashboard data comes via API routes.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
